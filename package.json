{
  "name": "dakroma-community",
  "version": "1.0.0",
  "description": "MrDakroma Community - Cyberpunk Messenger",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm run dev -- --host",
    "dev": "echo 'Open 2 terminals: npm run backend | npm run frontend'"
  }
}
